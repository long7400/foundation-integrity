interface:
  display_name: "Adversarial Foundation Review"
  short_description: "Independent session that tries to refute a foundation decision"
  default_prompt: "Use $adversarial-foundation-review in a separate session when an observable trigger fires; refute the receipt, construct the cheapest fake-pass implementation, separate solution correctness from document readiness, and return upholds/overturns/amends."
policy:
  allow_implicit_invocation: false
