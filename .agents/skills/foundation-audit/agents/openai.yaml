interface:
  display_name: "Foundation Audit"
  short_description: "Prove the foundation is sound before building on it"
  default_prompt: "Use $foundation-audit before design is frozen to falsify the load-bearing claims and system archetype, choose proof surfaces that can expose a fake pass, produce the classification/outcome receipt, and select one implementation route before building."
policy:
  allow_implicit_invocation: false
