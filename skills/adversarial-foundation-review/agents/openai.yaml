interface:
  display_name: "Adversarial Foundation Review"
  short_description: "Independent session that tries to refute a foundation decision"
  default_prompt: "Use $adversarial-foundation-review in a separate session whenever an observable foundation-surface, mismatch, or fitness trigger fires; refute the foundation-audit receipt's claims, evidence, outcome, and chosen route, then return upholds/overturns/amends."
policy:
  allow_implicit_invocation: false
