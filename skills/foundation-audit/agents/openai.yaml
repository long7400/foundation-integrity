interface:
  display_name: "Foundation Audit"
  short_description: "Prove the foundation is sound before building on it"
  default_prompt: "Use $foundation-audit to falsify the foundation claims this work depends on, produce a receipt and classification (OK/SUSPECT/BLOCKED), and choose one implementation route before building."
policy:
  allow_implicit_invocation: false
